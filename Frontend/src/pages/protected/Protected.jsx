// import { Navigate } from "react-router-dom";

// // A component to protect routes based on authentication and roles
// function ProtectedRoute({ isAuthenticated, role, role_name, children }) {
//   // Check if the user is authenticated and has the correct role
//   if (!isAuthenticated || role_name !== role) {
//     return <Navigate to="/" />;  // Redirect to login if not authenticated or role mismatch
//   }

//   return children;  // If authenticated and role matches, render the children (protected page)
// }

// export default ProtectedRoute;
